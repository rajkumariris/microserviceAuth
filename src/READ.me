//1.first signup with email and password using postman those get saved in db password stored as hash in db
//2.then signin with email and password and it again pwd converts to hash and check with db pwd if it is
//true create token and store it in session table and return token and user
//3. to headers of the postman and postman stores it in its headers and when we send again the request using
//productservice it goes along with headers stored in postman (basically it happens in browser)
